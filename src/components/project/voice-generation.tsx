/**
 * @deprecated Use VoiceSelectionPanel from "./voice-selection-panel" instead.
 * Kept as a re-export for backward compatibility.
 */
export {
  VoiceSelectionPanel as VoiceGeneration,
  type VoiceSelectionPanelProps as VoiceGenerationProps,
} from "./voice-selection-panel";
